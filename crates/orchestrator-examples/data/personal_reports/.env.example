# Optional: set these to use real SMTP instead of stub (writes to personal_reports_email.html).
# Same env vars as POC mailer. If unset, the example uses the stub mailer.
# SMTP=smtp.example.com
# SMTP_PORT=587
# SMTP_UNAME=user
# SMTP_PASS=secret
# DEFAULT_SENDER=you@example.com
# DEFAULT_SENDER_NAME=Personal Reports
# DEPLOY_ENV=production
